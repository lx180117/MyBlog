# 前端环境变量样例 —— 本地开发用；Docker 部署时这些由 docker-compose.yml 注入
#
# 生产环境的关键差别（务必理解，否则最容易踩）：
#   浏览器侧 API 地址应写成**同源相对路径** `/api/v1`，让 Nginx 去转发。
#   这样不需要 CORS，也不会把域名写死进构建产物（换域名不用重新构建镜像）。
#
# 但 SSR 跑在 Node 里，`$fetch` 解析不了相对路径（会抛 Failed to parse URL），
# 所以还需要一个**服务端专用**的绝对地址。它只存在于服务端进程，不会下发到浏览器。
#   本地开发：与 public 那个一致即可
#   容器部署：http://api:3000/api/v1（走 compose 内网服务名）

# 后端 API 根地址（含 /api/v1）—— 浏览器使用
NUXT_PUBLIC_API_BASE="http://localhost:3000/api/v1"

# 后端 API 根地址 —— 仅 SSR 使用（私有配置，不会下发到浏览器）
NUXT_API_BASE_SERVER="http://localhost:3000/api/v1"

# 站点对外地址，用于 canonical / og:url / 分享链接
NUXT_PUBLIC_SITE_URL="http://localhost:3001"
