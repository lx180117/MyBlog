# 构建上下文瘦身：不排掉的话，几百 MB 的 node_modules 会先被送进 Docker daemon，
# 每次 build 白等几分钟，而且会覆盖镜像里刚装好的依赖。
node_modules
.output
.nuxt
.data
dist

# 本地配置不进镜像（生产配置由 compose 的 environment 注入）
.env
.env.*
!.env.example

.git
.gitignore
Dockerfile
.dockerignore
*.log
npm-debug.log*
