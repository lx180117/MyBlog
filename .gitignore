# ── WorkBuddy 本地工作数据 ─────────────────────────────────────────
# .workbuddy/ 里是本机的工作痕迹：记忆日志(memory/)、构建日志、临时脚本、
# DTO 备份等。属于「本机运行时数据」，不是项目源码，不应进版本库。
# 需要把记忆日志也纳入版本管理的话，把下面这行换成 .workbuddy/tmp/ 即可。
.workbuddy/

# ── 依赖与构建产物 ──────────────────────────────────────────────
# server/.gitignore 与 client/.gitignore 已各自覆盖，这里是兜底，
# 防止在根目录误 add 时把几万个文件塞进暂存区。
node_modules/
dist/
build/
.nuxt/
.output/
.data/
coverage/

# ── 环境变量与密钥（绝不能提交） ────────────────────────────────
.env
.env.*
!.env.example

# ── 日志 ────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── 运行时产生的用户上传与数据库文件 ────────────────────────────
uploads/
*.sqlite
*.sqlite3
*.db

# ── 编辑器与系统文件 ────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── 压缩包（部署打包产物不入库） ────────────────────────────────
*.zip
*.tar.gz
